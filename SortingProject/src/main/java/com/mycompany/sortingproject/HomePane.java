
package com.mycompany.sortingproject;

import java.util.ArrayList;
import javafx.event.ActionEvent;

/**
 *
 * @author Ajay
 */
public class HomePane extends ListPane {
    public HomePane() {
        setDescription("\"Please select a sorting method from the menu. Remember that some methods are faster than others!\"");
        setName("Home Page");
        sizeField.setVisible(false);
    }
}
